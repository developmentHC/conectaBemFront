/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/libs/kubbClient";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/libs/kubbClient";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import type {
  GetMessagesContactsQueryResponse,
  GetMessagesContactsHeaderParams,
  GetMessagesContacts401,
  GetMessagesContacts403,
  GetMessagesContacts500,
} from "../types/GetMessagesContacts.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getMessagesContactsSuspenseQueryKey = () =>
  [{ url: "/messages/contacts" }] as const;

export type GetMessagesContactsSuspenseQueryKey = ReturnType<
  typeof getMessagesContactsSuspenseQueryKey
>;

/**
 * @description Lista todos os contatos (pacientes ou profissionais) do usuário autenticado, incluindo último envio e quantidade de mensagens não lidas.
 * @summary Listar contatos do usuário
 * {@link /messages/contacts}
 */
export async function getMessagesContactsSuspense(
  headers: GetMessagesContactsHeaderParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetMessagesContactsQueryResponse,
    ResponseErrorConfig<
      GetMessagesContacts401 | GetMessagesContacts403 | GetMessagesContacts500
    >,
    unknown
  >({
    method: "GET",
    url: `/messages/contacts`,
    ...requestConfig,
    headers: { ...headers, ...requestConfig.headers },
  });
  return res.data;
}

export function getMessagesContactsSuspenseQueryOptions(
  headers: GetMessagesContactsHeaderParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getMessagesContactsSuspenseQueryKey();
  return queryOptions<
    GetMessagesContactsQueryResponse,
    ResponseErrorConfig<
      GetMessagesContacts401 | GetMessagesContacts403 | GetMessagesContacts500
    >,
    GetMessagesContactsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getMessagesContactsSuspense(headers, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @description Lista todos os contatos (pacientes ou profissionais) do usuário autenticado, incluindo último envio e quantidade de mensagens não lidas.
 * @summary Listar contatos do usuário
 * {@link /messages/contacts}
 */
export function useGetMessagesContactsSuspense<
  TData = GetMessagesContactsQueryResponse,
  TQueryKey extends QueryKey = GetMessagesContactsSuspenseQueryKey,
>(
  headers: GetMessagesContactsHeaderParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetMessagesContactsQueryResponse,
        ResponseErrorConfig<
          | GetMessagesContacts401
          | GetMessagesContacts403
          | GetMessagesContacts500
        >,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getMessagesContactsSuspenseQueryKey();

  const query = useSuspenseQuery(
    {
      ...getMessagesContactsSuspenseQueryOptions(headers, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<
    TData,
    ResponseErrorConfig<
      GetMessagesContacts401 | GetMessagesContacts403 | GetMessagesContacts500
    >
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
